# Dependencies
node_modules/

# Environment variable files (NEVER commit your actual .env files)
.env
.env.development
.env.production
.env.test
# It's good practice to commit an example file if you have one
!.env.example

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Compiled JavaScript files if you're using TypeScript
# (adjust if your output directory is different, e.g., 'build/' or 'lib/')
# dist/
# *.js.map
# (Uncomment and adjust if your backend source is TypeScript and compiles to a folder)

# Coverage directory for tests
coverage/

# Uploads folder if you handle file uploads and store them temporarily/locally
# uploads/

# Operating System and IDE specific files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.iml
*.suo
*.user
*.userprefs
# Add any other editor or OS specific files you encounter


